# Screenshot Archive

This folder keeps the original screenshot inputs that were used before the public README images were replaced with polished generated result images.

- `current-project-assets/`: previous public README assets.
- `source-screenshots/`: plain iOS and macOS screenshots used as source material.

The public README uses only generated result images from `assets/`.
